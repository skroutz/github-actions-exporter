{
  "name": "github-actions-exporter",
  "version": "0.1.1",
  "description": "Prometheus exporter for GitHub Actions workflow/job metrics, authenticated via a GitHub App, polling all repos an installation has access to.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js",
    "lint": "node --check src/*.js test/*.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "octokit": "^4.1.2",
    "prom-client": "^15.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
